import React from 'react';

const styles = {
    container: {
        backgroundColor: 'white',
    },
};

const CalendarPreview = function ({startTime, endTime, status}) {
    return (
        <div >
        </div>
    )
};

export default CalendarPreview;